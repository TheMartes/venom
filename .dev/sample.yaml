---
kind: github.com/themartes/dotfiles

required: # pkgs that needs to be required
    - git

settings:
    git:
        - store-credentials
        - something
    custom:
        - "manual add" # some manual commands to run

scripts:
